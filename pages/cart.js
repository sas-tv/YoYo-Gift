import Cart from '../src/components/Cart';

/**
 * Helps render the cart page for a user
 * The user will get re-directed to login page, if he/she is not logged in and tries to visit this page
 * @returns 
 */
const CartPage = () => {
    return <Cart />
};

export default CartPage;